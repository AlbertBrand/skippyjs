#!/usr/bin/env node

var configFile = process.argv.slice(2);

require('babel/register')({
  only: new RegExp('skippyjs\/src|' + configFile)
});

module.exports = require(require('path').resolve(__dirname, '../src/main'));
